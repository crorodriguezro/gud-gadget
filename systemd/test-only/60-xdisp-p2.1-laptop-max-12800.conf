# XDISP-P2.1 controlled-laptop gates only.
#
# Cap the advertised maximum buffer size without changing default LZ4
# compression. Install/remove only through the component runbook's detached,
# receive-idle service-mutation boundary.
[Service]
Environment=GUD_TEST_MAX_BUFFER_SIZE=12800
