# Historical XDISP-P2.1 dynamic physical-mode matching gate only.
#
# E4-T03 promoted exact complete-timing routing to normal runtime. This file is
# retained only to reproduce old deployments; the environment value is now a
# compatibility no-op. Remove this drop-in from production service inventory.
[Service]
Environment=GUD_TEST_DYNAMIC_MODE_MATCH=1
Environment=RUST_LOG=debug
