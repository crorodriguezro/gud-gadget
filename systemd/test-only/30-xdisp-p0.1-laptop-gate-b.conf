# XDISP-P0.1 laptop Gate B only: OnePlus-length uncompressed payloads.
# Install only for this gate and remove it before normal use.
[Service]
Environment=GUD_TEST_COMPRESSION=none
Environment=GUD_TEST_MAX_BUFFER_SIZE=64000
