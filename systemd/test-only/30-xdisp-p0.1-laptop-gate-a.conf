# XDISP-P0.1 laptop Gate A only: 25-row control cadence with LZ4 retained.
# Install only for this gate and remove it before normal use.
[Service]
Environment=GUD_TEST_COMPRESSION=lz4
Environment=GUD_TEST_MAX_BUFFER_SIZE=64000
