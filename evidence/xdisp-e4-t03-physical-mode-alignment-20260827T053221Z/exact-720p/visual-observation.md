# 720p visual gate

E4-T02 already recorded operator-observed PASS on this phone/Pi/monitor path:
full-screen blue/white checkerboard, correct edges/colors, no crop or squeeze.
T03 reused an equivalent deterministic full-frame row-identity pattern and
proved `scaled=false` at the exact VC4 720p timing. No visual regression signal
appeared, so the prior physical observation remains the visual acceptance gate.
